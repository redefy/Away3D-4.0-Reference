Конечной целью проекта является выполнение двух пунктов:
1. Перевод официальной документации по Away3D 4.0 на русский язык.
2. Дописывание недостающих частей документации самостоятельно, дабы получить наиболее полную документацию по всем классам движка.

На данный момент дела обстоят так (попакетно):
	away3d.animators                || непереведен
	away3d.animators.data           || непереведен
	away3d.animators.skeleton       || непереведен
	away3d.animators.utils          || непереведен
	away3d.audio                    || переведен
	away3d.audio.drivers            || переведен
	away3d.bounds                   || переведен
	away3d.cameras                  || переведен
	away3d.cameras.lenses           || переведен
	away3d.containers               || переведен
	away3d.controllers              || переведен
	away3d.core.base                || переведен (кроме SkinnedSubGeometry)
	away3d.core.base.data           || переведен
	away3d.core.data                || переведен
	away3d.core.managers            || частично (в процессе)			
	away3d.core.math                || переведен
	away3d.core.partition           || частично (в процессе)	
	away3d.core.raycast             || непереведен
	away3d.core.raycast.colliders   || непереведен
	away3d.core.render              || непереведен
	away3d.core.sort                || непереведен
	away3d.core.traverse            || переведен
	away3d.debug                    || переведен	
	away3d.debug.data               || переведен
	away3d.entities                 || переведен
	away3d.errors                   || переведен
	away3d.events                   || частично	
	away3d.extrusions               || непереведен
	away3d.extrusions.utils         || непереведен
	away3d.filters                  || непереведен
	away3d.filters.tasks            || непереведен
	away3d.library                  || переведен
	away3d.library.assets           || переведен
	away3d.library.naming           || переведен
	away3d.library.utils            || переведен
	away3d.lights                   || непереведен
	away3d.lights.shadowmaps        || непереведен
	away3d.loaders                  || непереведен
	away3d.loaders.misc             || непереведен
	away3d.loaders.parsers          || непереведен
	away3d.loaders.parsers.data     || непереведен
	away3d.loaders.parsers.utils    || непереведен
	away3d.materials                || непереведен
	away3d.materials.lightpickers   || непереведен
	away3d.materials.methods        || непереведен
	away3d.materials.passes         || непереведен
	away3d.materials.utils          || непереведен
	away3d.primitives               || переведен
	away3d.primitives.data          || переведен
	away3d.textures                 || переведен
	away3d.tools                    || непереведен
	away3d.tools.commands           || непереведен
	away3d.tools.helpers            || непереведен
	away3d.tools.helpers.data       || непереведен
	away3d.tools.serialize          || непереведен
	away3d.tools.utils              || непереведен